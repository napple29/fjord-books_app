<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= Book.human_attribute_name(:title) %></br>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= Book.human_attribute_name(:memo) %></br>
    <%= form.text_area :memo %>
  </div>

  <div class="field">
    <%= Book.human_attribute_name(:author) %></br>
    <%= form.text_field :author %>
  </div>

  <div class="field">
    <%= Book.human_attribute_name(:picture) %></br>
    <%= form.file_field :picture %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
